<UserControl x:Class="ProjetClient.View.Authentification"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:ProjetClient.View"
             Background="{DynamicResource MaterialDesignPaper}"
             xmlns:viewmodel="clr-namespace:ProjetClient.ViewModel"
             d:DataContext="{d:DesignInstance Type= viewmodel:AuthentificationVM}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <WrapPanel Grid.Row="0" Orientation="Horizontal" Margin="9">
            <Label x:Name="label_nom" Content="Nom d'utilisateur:"/>
            <TextBox Background="{DynamicResource MaterialDesignPaper}"
                    Foreground="{DynamicResource MaterialDesignBody}"
                    FontFamily="{DynamicResource MaterialDesignFont}"
                    CaretBrush="{DynamicResource PrimaryHueMidBrush}"
                    SelectionBrush="{DynamicResource PrimaryHueLightBrush}"
                    Padding="8" Width="200" Text="{Binding Username}"/>
        </WrapPanel>

        <WrapPanel Grid.Row="1" Orientation="Horizontal" Margin="9">
            <Label x:Name="label_password" Content="Mot de passe:"/>
            <PasswordBox Width="200" PasswordChanged="PasswordBox_PasswordChanged"/>
        </WrapPanel>

        <Button x:Name="btn_connection" Grid.Row="2"  Margin="15" Content="Se connecter" Click="Button_Click"/>
    </Grid>
</UserControl>
